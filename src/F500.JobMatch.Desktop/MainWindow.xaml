<Window x:Class="F500.JobMatch.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="F500 Job Match" Height="600" Width="900" MinWidth="600" MinHeight="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <TextBox x:Name="FilePathTextBox"
                     Width="450"
                     Margin="0,0,12,0"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center"
                     ToolTip="Selected resume file" />
            <Button x:Name="BrowseButton"
                    Content="Browse…"
                    Width="90"
                    Click="BrowseButton_OnClick" />
            <Button x:Name="IngestButton"
                    Content="Ingest &amp; Match"
                    Margin="12,0,0,0"
                    Width="130"
                    Click="IngestButton_OnClick" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock Text="Top results:" VerticalAlignment="Center" />
            <TextBox x:Name="TopResultsTextBox"
                     Width="60"
                     Margin="8,0,0,0"
                     VerticalContentAlignment="Center"
                     Text="10" />
        </StackPanel>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="2"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap" />

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="12" Margin="0,0,0,12" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="{Binding TitleLine}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding ScoreLine}" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding Url}" Foreground="SteelBlue" Margin="0,4,0,0" TextDecorations="Underline" />
                                <TextBlock Text="{Binding Explanation}" Margin="0,8,0,0" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
